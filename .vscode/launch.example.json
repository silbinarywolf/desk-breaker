{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Debug (Native)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/zig-out/bin/desk-breaker",
            "preLaunchTask": "Zig Build Debug",
            "cwd": "${workspaceFolder}/zig-out/bin",
            "console": "internalConsole",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}